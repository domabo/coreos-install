#cloud-config

ssh_authorized_keys:
  - ssh-rsa <KEY>
  
users:
  - name: guy
    coreos-ssh-import-github: domabo
    sudo: ALL=(ALL) NOPASSWD:ALL
    groups: users, admin, sudo, docker
    lock-passwd: true

coreos:
  etcd:
      #generate a new token for each unique cluster from https://discovery.etcd.io/new
      discovery: https://discovery.etcd.io/61aae3a23ae6c3ba4116600de1cd414e
      addr: 192.73.233.85:4001
      peer-addr: 192.73.233.85:7001
  units:
    - name: etcd.service
      command: start
    - name: fleet.service
      command: start
      runtime: no
      content: |
        [Unit]
        Description=fleet

        [Service]
        Environment=FLEET_PUBLIC_IP=$public_ipv4
        ExecStart=/usr/bin/fleet
