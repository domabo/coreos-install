#cloud-config

coreos:
  etcd:
      #generate a new token for each unique cluster from https://discovery.etcd.io/new
      discovery: https://discovery.etcd.io/104a58f36755bb1f717a635a27ad19de
      addr: $public_ipv4:4001
      peer-addr: $public_ipv4:7001
  units:
    - name: etcd.service
      command: start
    - name: fleet.service
      command: start
      runtime: no
      content: |
        [Unit]
        Description=fleet

        [Service]
        Environment=FLEET_PUBLIC_IP=$public_ipv4
        ExecStart=/usr/bin/fleet

users:
  - name: guy
    coreos-ssh-import-github: domabo
    sudo: ALL=(ALL) NOPASSWD:ALL
    groups: users, admin, sudo, docker
    lock-passwd: true
